# ♻️ Spaced Repetition – Review Modules 01–05

## 🧪 Task
1. Create a table `accounts` with `id`, `owner`, `balance`, and a CHECK that balance >= 0.
2. Insert three rows.
3. Update the balance of one account (add 50).
4. Delete an account with zero balance (after setting one to 0).
5. Add a column `type TEXT` using `ALTER TABLE`.
6. Write a subquery that finds owners whose balance is greater than the average balance.
