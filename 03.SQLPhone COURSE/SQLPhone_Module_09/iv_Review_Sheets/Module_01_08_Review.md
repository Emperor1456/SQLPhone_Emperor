# ♻️ Spaced Repetition – Review Modules 01–08

## 🧪 Task (Python)
Write a Python script that:
1. Connects to a database `review.db`.
2. Creates a table `tasks` (id, description, done INTEGER DEFAULT 0).
3. Inserts three tasks using parameterized queries.
4. Updates one task to done=1.
5. Fetches and prints all tasks using `fetchall()`.
6. Handles any database errors gracefully.
