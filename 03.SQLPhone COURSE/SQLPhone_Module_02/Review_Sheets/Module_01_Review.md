# ♻️ Spaced Repetition – Review Module 01
**Before starting Module 02, refresh your Module 01 foundations.**

## 🧪 Task
1. Create a database `review.db` and a table `soldiers` with columns: `id INTEGER PRIMARY KEY`, `name TEXT NOT NULL`, `rank TEXT`.
2. Insert three rows (use different ranks).
3. Run a `SELECT` that shows only soldiers with the rank `'Captain'`.
4. Use dot‑commands to verify the table and its schema.

## ✅ Self‑Check
- [ ] Table was created with correct constraints.
- [ ] All three rows inserted.
- [ ] The `SELECT` returns exactly the captains.
